{
  "name": "todo",
  "version": "1.0.0",
  "description": "MERN Todo App",
  "scripts": {
    "install-all": "npm install --prefix client && npm install --prefix server",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm install && npm install --prefix client && npm install --prefix server && npm run build --prefix client && rm -rf server/public && mkdir -p server/public && cp -r client/dist/* server/public/",
    "start": "npm start --prefix server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
